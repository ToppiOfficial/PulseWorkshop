<Window x:Class="SrcWorkshop.App.Views.TextPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title, RelativeSource={RelativeSource Self}}"
        Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource BgBrush}">
    <StackPanel Margin="16">
        <TextBlock x:Name="PromptText" Text="Name" Style="{StaticResource MutedText}" Margin="0,0,0,6" />
        <TextBox x:Name="InputBox" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" IsCancel="True" Margin="0,0,8,0" Click="Cancel_Click" />
            <Button Content="OK" IsDefault="True" Style="{StaticResource AccentButton}" Click="Ok_Click" />
        </StackPanel>
    </StackPanel>
</Window>
